import { colors, radius, rankTierColors, spacing, typography } from "@ratlevel/ui";

export { colors, radius, rankTierColors, spacing, typography };

export const shadow = {
  card: {
    shadowColor: "#000000",
    shadowOffset: { width: 0, height: 12 },
    shadowOpacity: 0.28,
    shadowRadius: 22,
    elevation: 8
  }
} as const;
